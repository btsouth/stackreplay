/**
 * @stackreplay/adapters
 *
 * Local source adapters and normalization interfaces.
 *
 * Milestone 0: scaffolding only. No adapter logic lives here yet.
 *
 * Milestone 2 adds read-only, fixture-tested adapters for the first-class
 * sources (spec points 13-16): T3 Code, Hermes, Command Code, OpenCode, Codex,
 * Claude Code and ccusage JSON import. Adapters never modify source files,
 * never invent missing values, and emit deterministic dedupe fingerprints.
 */
export {};
